# =============================================================================
# PH Agent Hub — Membrane Tools (Stub)
# =============================================================================
# Full implementation deferred to a later phase.


def build_membrane_tools(tool_config: dict) -> list:
    """Build MAF tool callables for a Membrane backend.

    Args:
        tool_config: The ``config`` JSON dict from the Tool ORM record.

    Returns:
        An empty list (stub — not yet implemented).
    """
    return []
