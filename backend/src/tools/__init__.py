# =============================================================================
# PH Agent Hub — Tools Package
# =============================================================================
# Factory modules for building MAF @tool-decorated callables.
# Each module exposes a build_*_tools() function that returns a list of
# MAF tool callables bound to the relevant backend (ERPNext, Membrane, etc.).
